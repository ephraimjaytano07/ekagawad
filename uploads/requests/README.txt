Real resident-uploaded supporting documents (images/PDFs) are saved
here automatically by api/requests.php when a resident submits a
request. This folder must be writable by the web server
(e.g. chmod 755 or 775, owned by the web server user).
